#include<stdio.h>

int main(){
    int number;
    char current_digit;

    scanf("%s", &current_digit);
    scanf("%d", &number);
    printf("Char: %s", current_digit);
    printf("Int: %d", number);


}
